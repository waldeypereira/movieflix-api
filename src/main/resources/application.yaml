spring:
  application:
    name: movieflix

  datasource:
    url: jdbc:mysql://localhost:3306/movieflix?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: springstudent
    password: springstudent
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    show-sql: true

  flyway:
    enabled: true





